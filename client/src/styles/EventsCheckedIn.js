const EventsCheckedInStyles = theme => ({
  container: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
  },

  item: {
    backgroundColor: theme.palette.background.paper,
    margin: '1em',
    padding: '1em',
  }
});

export default EventsCheckedInStyles;
